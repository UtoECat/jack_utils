CURR_DIR := $(ROOT_DIR)/TARGET

SRCS := $(shell find $(SRC_DIR) -name '*.c' -or -name '*.cpp')
OBJS := $(SRCS:%=$(BLD_DIR)/%.o)
DEPS := $(OBJS:.o=.d)

INCFLAGS += $(addprefix -I,$(shell find $(SRC_DIRS) -type d))
CCFLAGS  += $(CMPFLAGS)
CXXFLAGS += $(CMPFLAGS) 
CLDFLAGS += $(LDLIBS)

